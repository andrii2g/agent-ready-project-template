# Output styles

Place project-scoped Claude Code output-style definitions in this directory only
when the project has a stable, reviewed presentation requirement. The baseline
template deliberately supplies no custom style so agent responses retain the
platform default.

Do not use output styles to weaken `AGENTS.md`, `CLAUDE.md`, permission rules,
hooks, or review requirements.
