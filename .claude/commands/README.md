# Legacy custom commands

Claude Code still supports project commands in this directory, but project
skills under `.claude/skills/<name>/SKILL.md` are the current preferred format.
This template deliberately keeps repeatable workflows as portable Agent Skills
and does not duplicate them here.
