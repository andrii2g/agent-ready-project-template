{
  "name": "typescript",
  "description": "Node.js TypeScript library/CLI baseline using ESLint, Prettier, Vitest, and strict compilation.",
  "commands": {
    "setup": "npm install",
    "format": "npm run format:check",
    "lint": "npm run lint",
    "typecheck": "npm run typecheck",
    "test": "npm test",
    "build": "npm run build",
    "verify": "python scripts/verify_repository.py && npm run format:check && npm run lint && npm run typecheck && npm test && npm run build"
  },
  "overwrites": [
    ".github/workflows/ci.yml",
    ".github/dependabot.yml"
  ]
}
